const Hero = () => {
	return (
		<section className="hero text-[3em] w-full text-center flex items-center justify-center">
			<h1 className="font-bold font-bricolage w-[300px] lg:w-full">
				How's the sky looking today?
			</h1>
		</section>
	);
};

export default Hero;
